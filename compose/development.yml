version: '3.1'

services:
  postgres:
    extends:
      file: base.yml
      service: postgres
    container_name: dev-go2-pg

  rabbitmq:
    extends:
      file: base.yml
      service: rabbitmq
    container_name: dev-go2-rabbitmq